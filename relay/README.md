# Kairo Relay

Servidor de encaminhamento para acesso remoto. Execute a partir da raiz do projeto com `npm run relay`.

Para uma hospedagem pública, configure HTTPS/WSS no provedor. O servidor mantém apenas conexões WebSocket em memória e não salva as telas.
